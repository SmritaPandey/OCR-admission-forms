fastapi==0.120.4
uvicorn[standard]==0.38.0
sqlalchemy==2.0.44
pydantic==2.12.3
pydantic-settings==2.11.0
pillow==12.0.0
pytesseract==0.3.13
tesserocr==2.7.3  # For Tesseract region detection (GetRegions API)
psycopg2-binary==2.9.11
python-multipart==0.0.20
requests==2.32.5
PyMuPDF==1.24.13
opencv-python==4.10.0.84
numpy==2.1.3
pdf2image==1.16.3
aiohttp==3.9.1

# AI Vision-Language Model Providers (optional - install as needed)
# openai==1.12.0  # For GPT-4 Vision
# anthropic==0.18.1  # For Claude Vision
# ollama==0.1.7  # For Ollama local models

# Training and ML (optional - uncomment to use)
# transformers>=4.36.0  # For TrOCR/Donut training
# torch>=2.1.0  # For local model inference and training
# datasets>=2.16.0  # For training data management
# accelerate>=0.25.0  # For distributed training

# CRAFT + TR-OCR for Handwritten Text Recognition (optional - install as needed)
# transformers>=4.36.0  # Required for TR-OCR
# torch>=2.1.0  # Required for CRAFT and TR-OCR
# torchvision>=0.16.0  # Required for image processing
# craft-text-detector>=0.4.2  # CRAFT text detection model

# Batch Processing (optional)
# redis==5.0.1  # For Redis queue backend
# celery==5.3.4  # For Celery task queue (alternative to batch_processor)

# OCR Providers (Install based on your choice)
# Google Cloud Document AI - BEST for handwriting and forms
google-cloud-documentai==2.20.0

# Google Cloud Vision - Excellent for general text and handwriting
google-cloud-vision==3.7.0

# Azure Form Recognizer - Excellent for structured forms
azure-ai-formrecognizer==3.3.0

# Azure Computer Vision
azure-cognitiveservices-vision-computervision==0.9.0
msrest==0.7.1

# AWS Textract - Good for forms and handwriting
boto3==1.34.0

